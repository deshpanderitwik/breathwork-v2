{
  "name": "breathe",
  "private": true,
  "version": "0.0.0",
  "description": "A breathing metronome with rounds. Three surfaces, one breath.",
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@8.15.5",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev:web": "pnpm --filter @breathe/web dev",
    "dev:core": "pnpm --filter @breathe/core dev"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
